# Dockerfile
FROM mcr.microsoft.com/playwright:v1.48.2-jammy

WORKDIR /app

# Install deps
COPY package.json package-lock.json* ./
RUN npm ci

# Copy sources
COPY . .

# Default port
ENV PORT=3000
EXPOSE 3000

# Start API
CMD ["npm", "run", "api"]
